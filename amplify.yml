version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci

    build:
      commands:
        - npm run build

  artifacts:
    baseDirectory: .next/standalone   # 🔥 CHANGE THIS
    files:
      - '**/*'

  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*